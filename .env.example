# Exemplos locais sem segredos reais.
DATABASE_URL="postgresql://planner_fin_local:planner_fin_local@localhost:5432/planner_fin_local?schema=public"
API_PORT=3000
API_CORS_ORIGINS=http://localhost:9000,https://localhost
VITE_API_BASE_URL=http://localhost:3000/api
JWT_SECRET=synthetic-local-key-32-bytes-minimum-do-not-reuse
REFRESH_HMAC_SECRET=another-synthetic-local-key-32-bytes-do-not-reuse
JWT_ISSUER=planner-fin-api
JWT_AUDIENCE=planner-fin-web
COOKIE_SECURE=false

# Android internal/release: configure somente localmente, nunca versionar keystore ou senhas.
# VITE_API_BASE_URL=https://api.example.test/api
# PLANNER_FIN_KEYSTORE_FILE=C:\caminho\privado\planner-fin.jks
# PLANNER_FIN_KEYSTORE_PASSWORD=
# PLANNER_FIN_KEY_ALIAS=planner-fin
# PLANNER_FIN_KEY_PASSWORD=

# Publisher local de releases Android (pnpm android:release:publish): credenciais do
# Railway Bucket, nunca expostas ao Web/Android nem versionadas.
# BUCKET=
# ENDPOINT=https://storage.railway.app
# REGION=
# ACCESS_KEY_ID=
# SECRET_ACCESS_KEY=
